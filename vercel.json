{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist/gorest-users-fe/browser",
  "github": {
    "silent": true
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "build": {
    "env": {
      "NG_APP_API_TOKEN": "@API_TOKEN",
      "NG_APP_API_URL": "@API_URL"
    }
  }
}
