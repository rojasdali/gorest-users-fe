{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist/gorest-users-fe/browser",
  "github": {
    "silent": true
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "build": {
    "env": {
      "API_TOKEN": "@API_TOKEN",
      "API_URL": "@API_URL"
    }
  }
}
